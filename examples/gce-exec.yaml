---
name: gce-exec example

watcher:
  type: gce
  config:
    source: instance
    key: foo
    metadata_url: http://localhost:8888/computeMetadata/v1/

executor:
  type: command
  config:
    command: |
        #!/bin/bash
        echo "Key changed!"
        echo "$GOVERSEER_DATA"
